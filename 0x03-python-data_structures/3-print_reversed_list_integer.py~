#!/usr/bin/python3
def print_reversed_list_integer(my_list=[]):
    if my_list is None:
        print('', end='')
    else:
        list.reverse()
        for index in range(len(my_list)):
            print("{:d}".format(list[index])
